import os
import sys
import time

def loadinTwo():
	while(True):
		os.sys("clear")
		time.sleep(1)
		sys.stdout.write("\r")
		sys.stdout.flush()

		sys.stdout.write("Loading")
		time.sleep(0.2)
		sys.stdout.flush()

		sys.stdout.write(".")
		time.sleep(0.2)
		sys.stdout.flush()

		sys.stdout.write(".")
		time.sleep(0.2)
		sys.stdout.flush()

		sys.stdout.write(".")
		time.sleep(0.2)
		sys.stdout.flush()

		sys.stdout.write(".")
		time.sleep(0.2)
		sys.stdout.flush()
                os.system("clear")
		sys.stdout.write("\r")


if __name__ == "__main__":
	loadinTwo()
